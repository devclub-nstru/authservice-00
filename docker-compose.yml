version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    command: /app/server
    ports:
      - "8000:8000"
    env_file:
      - .env

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: /app/worker
    env_file:
      - .env
